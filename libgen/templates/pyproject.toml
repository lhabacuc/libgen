[build-system]
requires = ["setuptools>=65", "wheel", "pybind11>=2.11"]
build-backend = "setuptools.build_meta"

[project]
name = "$module_name"
version = "0.1.0"
description = "Generated by libgen"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []
